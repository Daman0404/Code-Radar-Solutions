#include <stdio.h>
int  main()
{
    int a;
    scanf("%d", a);
    if (a>=z){
        printf("Lowercase");
    }
    if else (a>=Z){
        printf("Uppercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}