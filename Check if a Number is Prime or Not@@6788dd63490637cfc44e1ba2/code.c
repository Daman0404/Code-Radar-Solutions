#include <stdio.h>
int main()
{
    int a;
    scanf("%d", &a);
    if(a<=2){
        printf("Not Prime");
    }
    else if(a==0 || a==0 || a==0 || a==0){
        printf("Not Prime");
    }
    else if(a%2==0 || a%3==0 || a%4==0 || a%5==0){
        printf("Not Prime");}
return 0;

}